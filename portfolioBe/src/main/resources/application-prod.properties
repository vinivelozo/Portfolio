spring.config.import=optional:file:.env

# ? Use Railway MySQL configuration
spring.datasource.url=jdbc:mysql://${MYSQLHOST:mysql.railway.internal}:${MYSQLPORT:3306}/${MYSQLDATABASE:railway}
spring.datasource.username=root
spring.datasource.password=DEzQoQRRjWPiYdAVIjyANXyNVgmZDYwo

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect

# ? Ensure the backend starts correctly
server.port=8080

# ? Fix deprecated initialization mode
spring.sql.init.mode=never
spring.jpa.defer-datasource-initialization=true

# Mail configuration (if needed)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${SPRING_MAIL_USERNAME}
spring.mail.password=${SPRING_MAIL_PASSWORD}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

admin.username=${ADMIN_USERNAME}
admin.password=${ADMIN_PASSWORD}
